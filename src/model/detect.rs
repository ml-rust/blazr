//! Architecture detection — re-exports from boostr.
//!
//! Detection logic lives in boostr so it's available to all consumers
//! (blazr, compressr, oxidizr).

pub use boostr::model::detection::*;
